# adapters/stubs

Hand-written `.pyi` type stubs (PEP 561) for third-party libraries that ship no type
information (e.g. `h3`, `s2sphere`, DGGRID wrapper), so `mypy --strict` can check adapter calls.
